import SentReviewList from "@/components/domain/review/SentReviewList";

const SendReviewPage = ({ params }: { params: { slug: string } }) => {
  return <SentReviewList />;
};

export default SendReviewPage;
